# 计算机视觉学习笔记

这份笔记按当前仓库中已学习的 11 个示例整理，面向计算机视觉初学者。各节的“运行时观察”是复习建议，不是本次实测结果。整理笔记只阅读了代码，没有运行摄像头。

先记住两个基础概念：图像是像素数组，视频是一系列按时间排列的图像。OpenCV 的彩色图像通常使用 BGR 通道顺序；灰度图每个像素只有一个亮度值。图像中的 `x` 向右、`y` 向下，而数组访问顺序是 `image[y, x]`。

## 1. generic_interface.py：建立视频处理骨架

### 它解决什么问题

读取视频文件或摄像头画面，逐帧显示，并处理缩放、键盘操作和窗口关闭。理解这个骨架后，后续算法就是在“读取一帧”和“显示结果”之间加入处理步骤。

### 核心 OpenCV 函数

- `cv2.VideoCapture()`、`cap.open()`、`cap.read()`：打开输入并读取帧；读取失败时结束处理。
- `cv2.resize()`：调整图像尺寸。
- `cv2.namedWindow()`、`cv2.imshow()`、`cv2.waitKey()`：创建窗口、显示图像、处理窗口事件和键盘输入。
- `cv2.getTickCount()`、`cv2.getTickFrequency()`：计算处理时间。
- `cv2.destroyAllWindows()`：关闭窗口。

无视频文件参数时，代码会尝试使用 `camera_stream.CameraVideoStream()`，失败后退回 OpenCV 默认采集方式。

### 我运行时应该观察什么

画面是否连续、缩放是否改变尺寸、视频结束时是否退出。`x` 退出，`f` 切换全屏。代码根据处理耗时调整等待时间，目标节奏约为每帧 40 毫秒，但实际帧率也受输入和处理速度影响。

### 和前后算法的关系

这是所有示例的基础：先获得像素，再处理像素。下一步 Gaussian 会在这个骨架里加入滤波。

## 2. gaussian.py：用高斯滤波抑制噪声

### 它解决什么问题

用周围像素的加权平均平滑图像，减少细小噪声和纹理波动。代价是边界和细节也会变模糊。

### 核心 OpenCV 函数

- `cv2.GaussianBlur(frame, (N, N), 0)`：对彩色帧做高斯平滑；这里让 OpenCV 根据核大小确定 sigma。
- `cv2.createTrackbar()`、`cv2.getTrackbarPos()`：通过滑块改变核大小。
- `cv2.putText()`：显示处理耗时和由耗时推算的处理帧率。

代码把 `N` 限制为至少 3 的奇数，实际核大小可能与滑块读数略有不同。

### 我运行时应该观察什么

比较原图与平滑图。增大核大小后，噪点是否减少，文字、头发和物体边缘是否变模糊，处理耗时是否变化。理解“降噪”和“保留细节”的取舍。

### 和前后算法的关系

它把原始像素变得更平滑，为梯度和边缘检测做准备。下一步 Sobel 对亮度变化敏感，所以噪声也可能产生强响应；后面的 Canny 示例明确先做高斯平滑。

## 3. sobel.py：理解图像变化与梯度

### 它解决什么问题

用导数滤波突出像素值变化的位置，是理解边缘检测的入口。通常分别求水平导数 `Gx` 和垂直导数 `Gy`，再组合梯度强度。

### 核心 OpenCV 函数

- 本文件实际调用 `cv2.Sobel(frame, cv2.CV_8U, 1, 1, ksize=N)`。
- `cv2.getTrackbarPos()`：调整导数滤波核大小。

**注意实际实现：** `dx=1, dy=1` 计算的是混合导数，并不是分别计算 `Gx`、`Gy` 后合成梯度幅值。这里直接处理三个颜色通道，且输出为无符号 8 位，负响应会被截断，强响应也可能饱和。因此显示结果不是完整的带符号梯度。

### 我运行时应该观察什么

哪些物体边界和纹理变亮，平坦区域是否响应较弱。改变核大小后，响应范围和细节怎样变化。不要把画面直接理解为最终二值边缘图。

### 和前后算法的关系

Gaussian 侧重平滑，Sobel 侧重变化。下一步 Canny 会结合梯度、非极大值抑制和双阈值连接，把变化响应整理为更细的边缘。

## 4. canny.py：从变化响应得到边缘

### 它解决什么问题

提取物体边界，输出黑底白线的二值边缘图。Canny 通过非极大值抑制让边缘变细，再用双阈值和连接规则保留可靠边缘。

### 核心 OpenCV 函数

- `cv2.cvtColor(frame, cv2.COLOR_BGR2GRAY)`：转灰度。
- `cv2.GaussianBlur()`：先平滑降噪。
- `cv2.Canny(smoothed, lower, upper, apertureSize=sobel_size)`：检测边缘。

高阈值决定强边缘，低阈值决定可能被保留的弱边缘；弱边缘需要与强边缘连通才能留下。复习时保持低阈值小于高阈值。

### 我运行时应该观察什么

降低阈值后，细节和噪声是否一起增加；提高阈值后，边缘是否断裂或消失；增大平滑核后，小纹理是否减少。关注边缘的连续性，而不只是白线数量。

### 和前后算法的关系

它接续滤波和梯度，得到可供几何分析使用的边缘。下一步 Harris 学习的是局部关键点，并不需要把 Canny 输出作为输入；后面的 Hough 和轮廓示例则直接使用 Canny 边缘图。

## 5. harris.py：寻找稳定的角点

### 它解决什么问题

找到在多个方向上都有明显变化的局部位置，例如棋盘交点、窗框角和文字拐角。平坦区域各方向变化都小；直边主要在一个方向变化大；角点在两个方向都有明显变化。

### 核心 OpenCV 函数

- `cv2.cvtColor()`，配合 `np.float32()`：准备灰度浮点图像。
- `cv2.goodFeaturesToTrack(..., maxCorners=500, qualityLevel=0.01, minDistance=10, blockSize=N, useHarrisDetector=True, k=0.01)`：使用 Harris 判据选择角点。
- `cv2.circle()`：画绿色角点标记。

文件中的 `cv2.cornerHarris()` 只是注释里的替代方法，当前执行路径使用的是 `goodFeaturesToTrack()`。

### 我运行时应该观察什么

绿色点是否集中在拐角和纹理丰富处，而不是均匀分布在平坦墙面上。调节邻域大小，观察角点数量和位置变化；移动画面时，观察相同结构上的点是否稳定。

### 和前后算法的关系

角点与边缘都利用局部变化，但角点更适合作为定位和匹配的锚点。下一步 Hough 转向整体直线结构；后面的 SIFT 会进一步给关键点计算描述子，让点能够跨图像匹配。

## 6. houghlines.py：把边缘组织成直线

### 它解决什么问题

从边缘像素中检测直线结构，例如桌边、门框和道路标线。Hough 通过参数空间投票寻找被多个边缘点支持的直线。

### 核心 OpenCV 函数

- `cv2.cvtColor()`、`cv2.GaussianBlur()`、`cv2.Canny()`：生成边缘图。
- `cv2.HoughLines()`：返回直线参数 `(rho, theta)`，再换算成绘制端点。
- `cv2.HoughLinesP()`：概率 Hough，返回线段端点。
- `cv2.line()`：绘制绿色线条。

### 我运行时应该观察什么

默认使用标准 Hough，按 `p` 切换概率 Hough。比较延伸直线与有限线段的显示；调节 Canny 参数，观察边缘质量怎样影响直线结果。当前代码遍历的是 `lines[0]`，显示数量不一定代表全部检测结果。

### 和前后算法的关系

Harris 提供局部点，Hough 提供整体线结构，两者是不同的几何表示。下一步轮廓同样组织边缘，但能表示弯曲或不规则的边界，不局限于直线。

## 7. contour_edges.py：把边缘组织成轮廓

### 它解决什么问题

将二值边缘图整理成轮廓点集，并按面积找出最大轮廓。轮廓可以进一步用于形状分析和区域定位。

### 核心 OpenCV 函数

- `cv2.GaussianBlur()`、`cv2.Canny()`：生成边缘图。
- `cv2.findContours(canny, cv2.RETR_TREE, cv2.CHAIN_APPROX_SIMPLE)`：提取轮廓及嵌套层级，压缩冗余点。
- `cv2.contourArea()`：计算轮廓围成的面积。
- `cv2.drawContours()`：绘制轮廓。

### 我运行时应该观察什么

灰度底图上的绿色线显示所有轮廓，原图上的红色线显示面积最大的轮廓。调整阈值后，轮廓是否断裂、变碎或合并。最大轮廓不一定是想找的目标；边缘不闭合时，轮廓面积也可能不符合直觉。

### 和前后算法的关系

它与 Hough 都从边缘走向几何结构：前者描述一般形状，后者突出直线。下一步 SIFT 从“描述边界”转向“寻找能够在另一幅图中认出来的局部特征”。

## 8. sift_detection.py：关键点、描述子与特征匹配

### 它解决什么问题

提取对尺度和旋转变化较稳健的局部特征，将鼠标选中的模板区域与当前帧匹配，并可估计模板在画面中的位置。

关键点回答“在哪里”，描述子回答“附近长什么样”，匹配回答“两幅图里的哪些点可能对应”。

### 核心 OpenCV 函数

- `cv2.SIFT_create(400)`、`feature_object.detectAndCompute()`：创建 SIFT 并计算关键点和描述子。这里的 `400` 是保留特征数参数，不是代码注释所说的 Hessian 阈值。
- `cv2.drawKeypoints()`：显示关键点的尺度和方向。
- `cv2.FlannBasedMatcher()`／`cv2.BFMatcher()`、`matcher.knnMatch(..., k=2)`：寻找最近和次近的匹配。
- `cv2.drawMatches()`：显示匹配连线。
- `cv2.findHomography(..., cv2.RANSAC, 5.0)`、`cv2.perspectiveTransform()`：利用匹配点估计平面投影关系，并变换模板边框。

代码用 `m.distance < 0.7 * n.distance` 做比率检验，筛除最近匹配与次近匹配差别不明显的候选。单应性适合近似平面目标，不代表能解释任意三维物体的运动。

### 我运行时应该观察什么

从左上向右下拖动鼠标选择有纹理的区域，观察匹配连线是否连接相同细节。改变目标大小、方向或部分遮挡后，匹配是否保留。`d` 切换关键点显示，`h` 切换单应性定位；关注红色定位框是否稳定，以及匹配不足时的提示。

### 和前后算法的关系

轮廓提供形状，SIFT 提供局部外观的对应关系。它比单独检测 Harris 角点多了描述和匹配步骤。下一步光流也寻找帧间对应，但重点是相邻帧中的稠密像素位移。

## 9. opticflow.py：估计相邻帧的运动

### 它解决什么问题

估计前一帧到当前帧的二维图像位移，展示画面中哪些区域朝什么方向移动、移动多少。

### 核心 OpenCV 函数

- `cv2.cvtColor()`：把前后帧转成灰度。
- `cv2.calcOpticalFlowFarneback(prevgray, gray, None, 0.5, 3, 15, 3, 5, 1.2, 0)`：计算稠密光流。
- `cv2.polylines()`、`cv2.circle()`：抽样绘制绿色位移线段及起点。

输出 `flow` 的形状为 `(H, W, 2)`，每个位置保存 `(dx, dy)`。代码每轮更新 `prevgray`，所以比较的是相邻帧。

### 我运行时应该观察什么

静止区域的线段应较短，移动区域的线段应大致沿运动方向。线段长度表示帧间位移，不直接等于真实世界速度。整体画面移动会造成大范围光流；低纹理、快速运动和亮度变化可能使估计不可靠。

### 和前后算法的关系

SIFT 匹配的是稀疏关键点，Farneback 光流估计的是稠密位移。下一步背景减除不计算方向，而是学习背景后区分前景；两者都引入了时间信息。

## 10. mog-background-subtraction.py：学习背景并分离前景

### 它解决什么问题

通过多帧学习背景模型，找出与背景不同的区域，适合固定视角下的运动目标分离。这里使用的是 MOG2，即基于高斯混合模型的背景减除。

### 核心 OpenCV 函数

- `cv2.createBackgroundSubtractorMOG2(history=2000, varThreshold=16, detectShadows=True)`：创建背景模型。
- `mog.apply(frame)`：更新模型并输出前景掩膜。
- `mog.getBackgroundImage()`：取得当前背景图。
- `cv2.threshold(..., 200, 255, cv2.THRESH_BINARY)`：保留高值前景，排除阴影标签。
- `cv2.getStructuringElement()`、`cv2.erode()`、`cv2.dilate()`：用椭圆核处理小噪点和区域边界。

开启阴影检测后，掩膜通常用 0、127、255 表示背景、阴影、前景。窗口虽命名为“Foreground Probabiity”，显示的是类别掩膜，并不是连续概率。代码计算了膨胀结果 `fgdilated`，但最终前景窗口显示的是腐蚀结果 `fgeroded`。

### 我运行时应该观察什么

启动时模型尚未稳定，前景可能很多；随着学习，静态场景应逐渐成为背景。物体进入、停止或离开后，观察掩膜变化和残留“鬼影”。按空格重置模型，观察重新学习的过程；光照突变或相机移动可能产生大面积前景。

### 和前后算法的关系

光流回答“怎样移动”，背景减除回答“哪些区域不同于背景”。前景区域可以结合轮廓提取目标位置，作为跟踪观测。不过下一个 Kalman 示例实际使用颜色直方图和 CamShift，并没有直接调用 MOG2。

## 11. kalman_tracking_live.py：融合观测与运动预测

### 它解决什么问题

鼠标选定目标后，用颜色信息寻找目标位置，再用 Kalman 滤波结合运动模型进行预测。它将“每帧观察到的位置”与“根据历史运动推测的位置”连接起来。

### 核心 OpenCV 函数

- `cv2.cvtColor(..., cv2.COLOR_BGR2HSV)`、`cv2.inRange()`：准备颜色图像，并在建立模板时排除低饱和度或过暗区域。
- `cv2.calcHist()`、`cv2.normalize()`：建立并归一化目标的 H、S 二维颜色直方图。
- `cv2.calcBackProject()`：生成与目标颜色分布相似的响应图。
- `cv2.CamShift()`：在响应图中更新目标位置和搜索窗口。
- `cv2.boxPoints()`：取得 CamShift 旋转矩形的四个顶点。
- `cv2.KalmanFilter(4, 2)`、`kalman.correct()`、`kalman.predict()`：建立滤波器、用观测校正状态、预测下一步状态。

状态向量是 `[x, y, vx, vy]^T`，观测只有 `[x, y]^T`。转移矩阵使用每步时间间隔为 1 的匀速模型。当前代码顺序为先 `correct()`，再 `predict()`，并把下一步位置预测画在当前帧上。

### 我运行时应该观察什么

从左上向右下拖动选择颜色较鲜明的目标。蓝框是 CamShift 的观测，绿框是 Kalman 的预测。观察目标匀速移动时两者是否接近，突然转向时预测是否偏离，以及背景出现相似颜色时观测是否漂移。

Kalman 可以融合有噪声的观测，但不能凭空识别目标。这个示例仍持续依赖 CamShift 观测，没有完整的遮挡判定与丢失后重识别流程。

### 和前后算法的关系

它把前面学到的图像处理、外观表示和时间模型汇总到跟踪任务中。光流提供位移，背景减除提供前景，特征匹配提供对应关系；这些都可为跟踪提供线索，但本文件的具体路线是“颜色直方图 → 反向投影 → CamShift 观测 → Kalman 校正与预测”。

### 我修复过的问题：KalmanFilter.correct() 的输入 shape

**修复记录：我修复了 `kalman_tracking_live.py` 中传给 `KalmanFilter.correct()` 的观测输入 shape 问题。** 当前 `center(points)` 返回的不是一维坐标数组，而是二维列向量：

```python
return np.array([[x], [y]], dtype=np.float32)
```

调用位置是：

```python
kalman.correct(center(pts))
```

需要记住的区别：

| 写法 | shape | 含义 |
| --- | --- | --- |
| `np.array([x, y], dtype=np.float32)` | `(2,)` | 一维数组，不是显式列向量 |
| `np.array([[x, y]], dtype=np.float32)` | `(1, 2)` | 行向量 |
| `np.array([[x], [y]], dtype=np.float32)` | `(2, 1)` | 本例需要的观测列向量 |

本例观测矩阵 `measurementMatrix` 是 `(2, 4)`，把四维状态映射成二维位置观测。`correct()` 接收的观测应是 `(2, 1)`，且与这里使用的滤波器矩阵保持 `float32` 类型一致。调试时要同时检查数值、`shape` 和 `dtype`，不能只看“有两个坐标”。

另外，文件中的 `measurement = np.array((2, 1), np.float32)` 只是创建了包含数值 2 和 1 的一维数组，并不表示分配 `(2, 1)` 矩阵；它没有被传入当前的 `correct()` 调用。真正起作用的修复在 `center()` 的返回值。

## 最后总结：传统 CV 的学习主线

**像素 → 滤波 → 梯度 → 边缘 → 角点 → 轮廓/直线 → 特征匹配 → 运动估计 → 跟踪**

| 阶段 | 初学者应该理解的问题 | 对应示例 |
| --- | --- | --- |
| 像素 | 怎样读入并表示一帧图像？ | `generic_interface.py` |
| 滤波 | 怎样抑制噪声，同时尽量保留细节？ | `gaussian.py` |
| 梯度 | 哪里发生了明显变化？ | `sobel.py` |
| 边缘 | 怎样把变化整理成细而可靠的边界？ | `canny.py` |
| 角点 | 哪些局部位置适合定位？ | `harris.py` |
| 轮廓/直线 | 怎样把边缘组织成几何结构？ | `houghlines.py`、`contour_edges.py` |
| 特征匹配 | 两幅图中哪些局部区域相互对应？ | `sift_detection.py` |
| 运动估计 | 相邻帧怎样变化，哪些区域属于前景？ | `opticflow.py`、`mog-background-subtraction.py` |
| 跟踪 | 怎样结合观测和运动模型持续估计目标状态？ | `kalman_tracking_live.py` |

这条主线是理解顺序，不是所有算法必须串联执行的流水线。角点与轮廓/直线是不同的结构表示；背景减除是前景分离，并不直接给出运动向量；Kalman 是状态估计器，需要其他方法提供观测。

复习每个示例时，依次问自己：输入是什么，输出是什么，参数改变了什么，什么情况下结果会失败。传统 CV 的核心，是逐步把像素转换成结构、对应关系和时间状态。
